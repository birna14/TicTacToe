apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'is.ru.hugb.Game'

repositories {
        mavenCentral()
}

dependencies {
        testImplementation 'junit:junit:4.12'

        compile 'com.sparkjava:spark-core:2.6.0'
}

run {
    standardInput = System.in
}
